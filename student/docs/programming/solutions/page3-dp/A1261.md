---
layout: doc
---

# A1261 导弹拦截 II

---

## 📖 题目描述

最少拦截系统数量（优化版）。

**难度**: 提高  
**标签**: DP、贪心

---

## 💡 思路分析

### 算法思路

（此处待补充详细思路分析）

### 关键点

- 理解问题核心
- 设计合适的算法
- 注意边界条件

---

## 📝 代码实现

### C语言版本

```c
// C语言版本待补充
```

### C++版本

```cpp
/**
 * A1261: 拦截导弹
 * 最长不上升子序列问题
 */

#include &lt;bits/stdc++.h&gt;
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    
    int n;
    cin &gt;&gt; n;
    
    vector&lt;int&gt; h(n);
    for (int i = 0; i &lt; n; i++) {
        cin &gt;&gt; h[i];
    }
    
    // dp[i]: 以第i个导弹结尾的最长不上升子序列长度
    vector&lt;int&gt; dp(n, 1);
    int ans = 1;
    
    for (int i = 1; i &lt; n; i++) {
        for (int j = 0; j &lt; i; j++) {
            if (h[j] &gt;= h[i]) {
                dp[i] = max(dp[i], dp[j] + 1);
            }
        }
        ans = max(ans, dp[i]);
    }
    
    cout &lt;&lt; ans &lt;&lt; endl;
    
    return 0;
}

```

---

## ⏱️ 复杂度分析

| 指标 | 复杂度 | 说明 |
|:---|:---:|:---|
| 时间 | 待分析 | - |
| 空间 | 待分析 | - |

---

## 🎯 关键点总结

1. **理解题意** — 仔细阅读题目要求
2. **设计算法** — 选择合适的数据结构和算法
3. **注意细节** — 边界条件和特殊情况

---

## 🤔 扩展思考

1. 能否优化时间复杂度？
2. 能否优化空间复杂度？
3. 如果数据范围变大，该如何处理？

---

## 🔗 相关题目

- 无

---

> **「学而时习之，不亦说乎」** —— 多练习，多总结，方能融会贯通。
